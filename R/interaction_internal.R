# Downsamples a matrix, returns vector of row (cell) names
#
# @param matrix
# @param downsample.options

DownsampleMatrix <- function(mat, ...) {
  # super cool downsampling code here
  cells <- vector()
  return(cells)
}
